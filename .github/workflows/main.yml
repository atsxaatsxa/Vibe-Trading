name: Run Jobs Container

on:
  push:
    branches: [ main ]
  workflow_dispatch: # 支持手动点击运行

jobs:
  test-job:
    runs-on: ubuntu-latest
    container: ubuntu:22.04
    steps:
      - name: 容器内执行命令
        run: apt update && echo "容器运行成功"            
